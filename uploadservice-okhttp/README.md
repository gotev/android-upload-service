# Upload Service OkHttp module
This module has been discontinued since release 3.1, because the OkHttp implementation has been moved into the core Upload Service library and it's the default HTTP stack.
